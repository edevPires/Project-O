# Project-O
 
Para instalar as dependecias digite no terminal do vscode:

npm install

Para inicializar o compilador do tailwind para q os estilos se apliquem digite no terminal do vscode:

npm run tailinit

após seguir todos esses passos abra o index.html e inicialize ele com o live server.

enjoy!
